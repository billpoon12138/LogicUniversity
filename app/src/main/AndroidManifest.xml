<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student.logicuniversity">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity" android:label="LOGIC UNIVERSITY">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".MenuDepartment">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <activity android:name=".MenuStoreActivity">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <activity android:name=".RetrievalActivity" />
        <activity android:name=".RejectionDepartment" />
        <activity android:name=".DisbursementActivity" />
        <activity android:name=".StoreMenuActivity" />
        <activity android:name=".DepartmentRequisitionActivity" />
        <activity android:name=".CollectionDepartmentMulti" />
        <activity android:name=".EmployeeDepartment" />
        <activity android:name=".DistributeDepartment" />

        <activity android:name=".QRListActivity" android:label="ITEM DETAILS">
        </activity>

        <activity android:name=".DisbursementActivity2"></activity>

    </application>

</manifest>